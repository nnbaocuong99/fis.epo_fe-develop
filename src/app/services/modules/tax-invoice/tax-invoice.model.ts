import { BaseResponse } from '../../common/http/base-response.model';

export class TaxInvoiceDto extends BaseResponse {


    constructor(model?: any) {
        super(model);
        if (model) {

        }
    }
}
